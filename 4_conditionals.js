// The if statement
if (condition) {
    //code
}

// The else statement
if (condition) {
    //code
} else {
    //code
}

// The else if statement
if (condition) {
    //code
} else if(condition2) {
    //code
} else {
    //code
}

// The switch Statement
switch(expression) {
    case x:
        //code
        break;
    case y:
        //code
        break;
    default:
        //code
}

// Ternary Operator(? :) - used as short hand if...else
condition? expression1 : expression2 
const text = (age < 18) ? "Minor" : "Adult";